This was taken from a case where a feature module was augmented with a custom
cTools plugin that creates a block containing view results (teasers for
blog nodes). It includes a template file (not sure on naming of that file)
Where the custom block is printed within custom markup.
